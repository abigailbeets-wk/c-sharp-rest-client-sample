{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/sample-rest-client.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}